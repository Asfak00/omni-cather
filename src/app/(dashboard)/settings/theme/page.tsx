import { ThemeEditor } from "@/components/settings/theme-editor";

export const metadata = { title: "Theme Settings | Event Manager" };

export default function ThemeSettingsPage() {
  return <ThemeEditor />;
}
